# Projet Ft_printf for 42

Ce projet a pour but de réécrire la fonction prinf, sans le système de buffering.

## Compilation

Inclure le fichier `ft_printf.h` a votre projet, puis compiler le avec `make`.
